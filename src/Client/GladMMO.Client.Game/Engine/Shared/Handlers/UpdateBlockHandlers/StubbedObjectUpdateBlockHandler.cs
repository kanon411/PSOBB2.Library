using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Text;

namespace GladMMO
{
	/*public sealed class StubbedObjectUpdateBlockHandler : IObjectUpdateBlockHandler
	{
		/// <inheritdoc />
		public ObjectUpdateType UpdateType { get; }

		/// <inheritdoc />
		public StubbedObjectUpdateBlockHandler(ObjectUpdateType updateType)
		{
			if(!Enum.IsDefined(typeof(ObjectUpdateType), updateType)) throw new InvalidEnumArgumentException(nameof(updateType), (int)updateType, typeof(ObjectUpdateType));
			UpdateType = updateType;
		}

		/// <inheritdoc />
		public void HandleUpdateBlock(ObjectUpdateBlock updateBlock)
		{

		}
	}*/
}
